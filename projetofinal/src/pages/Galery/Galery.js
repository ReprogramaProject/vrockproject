import React from 'react'
import './Galery.css'

class Galery extends React.Component{

    render() {
        
        return (
            <div className="galery">
            
             <p> Galeria VRock </p> 

             <br/>
             <p>Use a Local Server For the options below, we should develop projects using a local server so that files are properly served. Options of local servers include: Downloading the Mongoose application and opening it from the same directory as your HTML file. Running python -m SimpleHTTPServer (or python -m http.server for Python 3) in a terminal in the same directory as your HTML file.
            Running npm install -g live-server  live-server in a terminal in the same directory as your HTML file.

            </p>
                    


            </div>
        )
    }

}

export default Galery